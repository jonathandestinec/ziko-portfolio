import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ["static.readdy.ai", "res.cloudinary.com", "ucarecdn.com", "images.unsplash.com", "lh3.googleusercontent.com"],
  },
};

export default nextConfig;
